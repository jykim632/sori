# 2025-01-03 작업 일지

## 프로젝트 점검

CLAUDE.md에 추가된 코딩 원칙에 따라 프로젝트를 점검했습니다.

### 타입 체크 결과

8개 에러 발견 → **모두 수정 완료** (PR #43)

| 파일                        | 문제                                       | 해결                           |
| --------------------------- | ------------------------------------------ | ------------------------------ |
| `widget.ts`                 | 미사용 import (`dirname`, `fileURLToPath`) | 제거                           |
| `feedbacks.tsx`             | `useRef` 초기값 누락 (React 19)            | 초기값 추가                    |
| `feedback.ts`, `project.ts` | `metadata`/`widgetConfig` 타입 불일치      | `JsonValueSchema` 생성 및 적용 |
| `organization.ts`           | 미사용 `User` import                       | 제거                           |

### SOLID 원칙 점검

#### 위반 사항

| 원칙 | 파일                       | 문제                                     |
| ---- | -------------------------- | ---------------------------------------- |
| SRP  | `$projectId.tsx` (1,081줄) | 기본정보, API키, 테마, 삭제 등 여러 기능 |
| SRP  | `feedbacks.tsx` (623줄)    | 필터바, 테이블, 페이지네이션 혼재        |
| SRP  | `organization.ts`          | 조직 CRUD + 웹훅 포맷팅 혼재             |
| OCP  | `formatWebhookPayload`     | Slack/Discord/Telegram if-else 분기      |

#### 준수 사항

- 파일명 kebab-case ✅
- 폴더 그룹화 (`components/admin/`, `server/`) ✅
- `index.ts` public API ✅
- `types.ts` 분리 ✅
- Zod 스키마 사용 ✅

### 함수 설계 점검

- 20줄 이내 위반: `ProjectSettingsPage` (~600줄), `FeedbacksPage` (~400줄), `formatWebhookPayload` (~90줄)

### 테스트 점검

- 테스트 파일 없음 (`*.test.ts`, `*.spec.ts`)
- 외부 의존성 직접 import (주입 불가)

---

## 생성된 이슈

| #   | 제목                                     | 라벨             | 우선순위 |
| --- | ---------------------------------------- | ---------------- | -------- |
| #38 | fix: TypeScript 타입 에러 수정           | `type: bug`      | high     |
| #39 | refactor: SRP 위반 컴포넌트 분리         | `type: refactor` | medium   |
| #40 | refactor: 웹훅 포맷터 Strategy 패턴 적용 | `type: refactor` | low      |
| #41 | refactor: 설정 데이터 하드코딩 분리      | `type: refactor` | low      |
| #42 | test: 테스트 코드 추가                   | `type: chore`    | medium   |

---

## 완료된 작업

### PR #43: TypeScript 타입 에러 수정 (머지 완료)

**변경 파일:**

- `apps/web/src/routes/api/v1/widget.ts` - 미사용 import 제거
- `apps/web/src/routes/admin/feedbacks.tsx` - useRef 초기값 추가
- `packages/database/src/schemas/json.ts` - JsonValueSchema 신규 생성
- `packages/database/src/schemas/feedback.ts` - metadata 타입 수정
- `packages/database/src/schemas/project.ts` - widgetConfig 타입 수정
- `packages/database/src/schemas/index.ts` - json.ts export 추가
- `packages/database/src/types.ts` - JsonValue 타입 export
- `packages/database/src/queries/organization.ts` - 미사용 User import 제거
- `apps/web/src/components/admin/*` - 신규 컴포넌트 추가

**JsonValueSchema 정의:**

```typescript
type JsonPrimitive = string | number | boolean | null;
type JsonArray = JsonValue[];
type JsonObject = { [key: string]: JsonValue };
export type JsonValue = JsonPrimitive | JsonArray | JsonObject;

export const JsonValueSchema: z.ZodType<JsonValue> = z.lazy(() =>
  z.union([
    z.string(),
    z.number(),
    z.boolean(),
    z.null(),
    z.array(JsonValueSchema),
    z.record(z.string(), JsonValueSchema),
  ])
);
```

### PR #44: Admin 페이지 탭 네비게이션 구조 개선 (머지 완료)

**변경 사항:**

- 피드백/프로젝트/설정 탭으로 Admin 페이지 분리
- `/admin/` 접근 시 `/admin/feedbacks`로 리다이렉트
- 프로젝트 목록 페이지 (생성/수정/삭제/임베드 코드 복사)
- 설정 페이지 (웹훅 관리, 조직 정보)

**변경 파일:**

- `apps/web/src/routes/admin.tsx` - 탭 네비게이션 레이아웃 추가
- `apps/web/src/routes/admin/index.tsx` - 리다이렉트 (신규)
- `apps/web/src/routes/admin/projects.tsx` - 프로젝트 목록 (신규)
- `apps/web/src/routes/admin/settings.tsx` - 설정 페이지 (신규)

→ Closes #39

---

### PR #45: 인증 페이지 라우트 그룹화 (머지 완료)

TanStack Router의 route groups 기능을 사용하여 인증 관련 페이지를 `(auth)/` 폴더로 그룹화.
URL 구조는 변경 없음 (`/login`, `/signup` 등 유지).

**변경 파일:**

- `routes/login.tsx` → `routes/(auth)/login.tsx`
- `routes/signup.tsx` → `routes/(auth)/signup.tsx`
- `routes/verify-email.tsx` → `routes/(auth)/verify-email.tsx`
- `routes/onboarding.tsx` → `routes/(auth)/onboarding.tsx`

**최종 구조:**

```
routes/
├── (auth)/              # 인증 페이지 그룹
│   ├── login.tsx
│   ├── signup.tsx
│   ├── verify-email.tsx
│   └── onboarding.tsx
├── admin.tsx + admin/   # 관리자 페이지
├── api/                 # API (그대로)
└── ...
```

→ Closes #36

---

### PR #46: 웹훅 포맷터 Strategy 패턴 적용 (머지 완료)

OCP 원칙에 따라 웹훅 포맷터를 Strategy 패턴으로 리팩토링.
`organization.ts`, `webhook.ts` 중복 코드 제거 (~175줄 → 공통 모듈).

**신규 구조:**

```
lib/webhook/
├── formatters/
│   ├── slack.ts      # Slack Block Kit
│   ├── discord.ts    # Discord Embed
│   ├── telegram.ts   # Telegram HTML
│   ├── generic.ts    # 기본 JSON
│   └── index.ts      # Factory (getFormatter)
├── types.ts          # 타입 정의
├── utils.ts          # 공통 유틸
└── index.ts          # Public API
```

새 서비스 추가 시 포맷터만 추가하면 됨 (기존 코드 수정 불필요).

→ Closes #40

---

## 이슈 상태 요약

| #   | 제목                                     | 상태             |
| --- | ---------------------------------------- | ---------------- |
| #36 | refactor: API 라우트 폴더 구조 정리      | ✅ 완료 (PR #45) |
| #39 | refactor: SRP 위반 컴포넌트 분리         | ✅ 완료 (PR #44) |
| #40 | refactor: 웹훅 포맷터 Strategy 패턴 적용 | ✅ 완료 (PR #46) |
| #41 | refactor: 설정 데이터 하드코딩 분리      | 🔲 미진행        |
| #42 | test: 테스트 코드 추가                   | 🔲 미진행        |

---

### 프로젝트 라우팅 수정

**문제:** 프로젝트 목록에서 "위젯 설정" 버튼 클릭 시 `$projectId.tsx` 페이지가 렌더링되지 않음.

**원인:** `admin/projects.tsx`가 `$projectId.tsx`의 부모 라우트로 인식되어 `Outlet`이 필요했으나 없었음.

**해결:** `projects.tsx` → `projects/index.tsx`로 이동하여 형제 라우트로 변경.

```
routes/admin/
├── projects/
│   ├── index.tsx      # /admin/projects (목록)
│   └── $projectId.tsx # /admin/projects/:id (설정)
```

---

### API 키 버그 수정 (커밋 72e3ef9)

**문제:** API 키 발급 후 페이지 새로고침하면 발급된 키가 사라짐.

**원인:** `getProjectById` 쿼리에서 `api_key` 필드를 SELECT하지 않아 로더에서 `null` 반환.

**해결:** `packages/database/src/queries/project.ts`의 `getProjectById`에 필드 추가:

```sql
SELECT
  p.id, p.name, p.allowed_origins, p.widget_config, p.organization_id,
  p.api_key as "apiKey", p.api_key_created_at as "apiKeyCreatedAt",  -- 추가
  p.created_at, p.updated_at,
  ...
```

---

## 다음 작업

1. 리팩토링 이슈 처리 (#41)
2. 테스트 인프라 구축 (#42)
3. develop → main 머지 (릴리즈)
